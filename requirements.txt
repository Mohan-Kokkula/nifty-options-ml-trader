requests>=2.28.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
tradingview-datafeed>=2.0.0    # TradingView data (pip install tradingview-datafeed)
# Kotak Neo broker SDK v2 (TOTP flow). NOT on PyPI, and its GitHub repo is
# not anonymously clonable from an isolated Docker build sandbox (no access
# to host git credentials). Vendored instead — see vendor/neo_api_client/
# and the corresponding COPY step in the Dockerfile. Do NOT add a
# `neo_api_client @ git+...` line back here; pip install will fail.
pyotp>=2.9.0                   # TOTP auto-generation for automated login
python-dotenv>=1.0.0           # loads config/settings.env on startup
feedparser>=6.0.10             # 24/7 news agent — RSS: MoneyControl, ET, LiveMint, BS
beautifulsoup4>=4.12.0         # SEBI press-release scrape (no RSS feed)
# News sentiment scoring — pick ONE provider:
#   FinBERT (default, local, zero cost, zero rate limits, ~420MB download):
transformers>=4.40.0
torch>=2.1.0
#   Cloud fallback (optional, only if NEWS_LLM_PROVIDER=anthropic):
# anthropic>=0.34.0

# Pre-market briefing agent (free LLM via Groq + global market data via yfinance)
groq>=0.11.0                   # Llama 3.3 70B free tier (14,400 req/day) — pre-market regime call
yfinance>=0.2.40               # Global indices, India VIX, Gift Nifty for pre-market briefing
